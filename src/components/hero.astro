---
import { Picture } from "astro:assets";
import heroImage from "@/assets/hero.png";
import whatIsImage from "@/assets/what_is_namdarine.png";
import Link from "@/components/ui/link.astro";
import Cta from "@/components/cta.astro";
import { Icon } from "astro-icon/components";

const lang = Astro.url.pathname.startsWith('/ko') ? 'ko' : 'en';
---

<main class="pt-12 sm:pt-16 pb-16 sm:pb-24 space-y-16 sm:space-y-24">
  <!-- Hero Section -->
  <section class="grid grid-cols-1 lg:grid-cols-2 place-items-center gap-8 px-4 sm:px-6">
    <!-- Text -->
    <div>
      <h1 class="text-3xl sm:text-5xl lg:text-6xl xl:text-7xl font-bold tracking-tight text-slate-800 leading-tight">
        {
          lang === "ko"
            ? "이미 완성된 것처럼, 지금 바로 당신만의 AI를 만드세요."
            : "Let's build it like it's already happened."
        }
      </h1>
      <p class="text-base sm:text-lg mt-6 text-slate-600 max-w-xl leading-relaxed"
        set:html={
          lang === "ko"
            ? `namdarine은 누구나 코딩 없이 나만의 AI를 만들 수 있도록 돕는 브랜드입니다.<br />
              자동화된 ML 워크플로우와 시각적 템플릿을 통해, 복잡함은 덜고 가능성은 넓혔습니다.<br />
              모듈처럼 조립하고, 흐름처럼 연결하세요. AI는 이제, 누구의 것이든 될 수 있습니다.`
            : "namdarine is building a world where anyone can create AI without code. From automated ML workflows to visual templates, we're making AI more accessible — modular, intuitive, and powerful."
        }
      />
      <div class="mt-6 flex flex-col sm:flex-row gap-3 sm:gap-4">
        {lang === "ko" ? (
          <Link
            href="#"
            size="lg"
            style="outline"
            class="flex gap-2 items-center justify-center pointer-events-none opacity-50 text-sm sm:text-base"
          >
          <Icon class="text-white w-5 h-5" name="bx:bx-grid-alt" />
          곧 만나요
        </Link>
        ) : (
          <a
            href="/en/white_paper.pdf"
            size="lg"
            style="outline"
            target="_blank"
            rel="noopener noreferrer"
            class="flex gap-2 items-center justify-center text-sm sm:text-base"
            style="text-decoration: none;"
          >
            <Icon class="text-white w-5 h-5" name="bx:bx-grid-alt" />
            Read Our Philosophy
          </a>
        )}
        
        <Link href={lang === "ko" ? "/content/ko/about" : "/about"} size="lg" style="outline" class="flex gap-2 items-center justify-center text-sm sm:text-base">
          <Icon class="text-black w-4 h-4" name="bx:bx-book-heart" />
          {lang === "ko" ? "namdarine이 지향하는 길" : "The Path namdarine Believes In"}
        </Link>
      </div>
    </div>

    <!-- Hero 이미지 -->
    <div class="py-4 sm:py-6">
      <Picture
        src={heroImage}
        alt="Modular AI illustration"
        widths={[300, 600, 900]}
        sizes="(max-width: 768px) 100vw, 600px"
        class="mx-auto w-full max-w-xs sm:max-w-md rounded-xl shadow-sm"
      />
    </div>
  </section>

  <!-- What is Namdarine -->
  <section class="px-4 sm:px-6">
    <h2 class="text-center text-xl sm:text-2xl font-semibold text-slate-700 mb-6">
      {lang === "ko" ? "namdarine이 그리는 세상은?" : "The World namdarine Envisions"}
    </h2>
    <Picture
      src={whatIsImage}
      alt={lang === "ko"
        ? "누구나 AI를 만들 수 있는 세상을 그리는 Namdarine"
        : "namdarine, creating a world where anyone can build AI"}
      widths={[400, 800, 1200]}
      sizes="(max-width: 768px) 100vw, 800px"
      class="mx-auto w-full max-w-2xl sm:max-w-4xl rounded-xl shadow-sm"
    />
    <p class="text-center text-slate-600 text-base sm:text-lg mt-4 leading-relaxed">
      {lang === "ko"
        ? "namdarine은 기술의 진입장벽을 낮추고, 누구나 자신의 방식대로 AI를 만들 수 있는 세상을 엽니다."
        : "namdarine lowers the barrier of entry — so anyone can build AI, their way."}
    </p>
  </section>
</main>