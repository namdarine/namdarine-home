---
import { Picture } from "astro:assets";
import heroImage from "@/assets/hero.png";
import whatIsImage from "@/assets/what_is_namdarine.png";
import Link from "@/components/ui/link.astro";
import Cta from "@/components/cta.astro";
import { Icon } from "astro-icon/components";
---

<main class="pt-16 pb-24 space-y-24">
  <!-- Hero Section with Static Image -->
  <section class="grid lg:grid-cols-2 place-items-center gap-8">
    <!-- Text Area -->
    <div>
      <h1 class="text-5xl lg:text-6xl xl:text-7xl font-bold tracking-tight text-slate-800">
        Let's build it like it's already happened.
      </h1>
      <p class="text-lg mt-6 text-slate-600 max-w-xl">
        Namdarine is building a world where anyone can create AI without code.
        From automated ML workflows to visual templates, we're making AI more accessible — modular, intuitive, and powerful.
      </p>
      <div class="mt-8 flex flex-col sm:flex-row gap-4">
        <Link href="#" class="flex gap-2 items-center justify-center pointer-events-none opacity-50">
          <Icon class="text-white w-5 h-5" name="bx:bx-grid-alt" />
          Coming Soon
        </Link>
        <Link href="/about" size="lg" style="outline" class="flex gap-2 items-center justify-center">
          <Icon class="text-black w-4 h-4" name="bx:bx-book-heart" />
          Learn the Philosophy
        </Link>
      </div>
    </div>

    <!-- Static Hero Image (replaceable with Rive later) -->
    <div class="py-6">
      <Picture
        src={heroImage}
        alt="Modular AI illustration"
        widths={[300, 600, 900]}
        sizes="(max-width: 800px) 100vw, 600px"
        class="mx-auto max-w-md rounded-xl shadow-sm"
      />
    </div>
  </section>

  <!-- What is Namdarine Image -->
  <section>
    <h2 class="text-center text-2xl font-semibold text-slate-700 mb-6">
      What is Namdarine?
    </h2>
    <Picture
      src={whatIsImage}
      alt="What is Namdarine?"
      widths={[400, 800, 1200]}
      sizes="(max-width: 768px) 100vw, 800px"
      class="mx-auto w-full max-w-4xl rounded-xl shadow-sm"
    />
  </section>

  <!-- <Cta /> -->
</main>
