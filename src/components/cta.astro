---
import Link from "./ui/link.astro";

const lang = Astro.url.pathname.startsWith('/ko') ? 'ko' : 'en';
---

<section class="bg-black p-10 mt-24 text-center text-white rounded-lg max-w-4xl mx-auto">
  <h2 class="text-3xl md:text-4xl font-bold">
    {lang === "ko" ? "클릭 몇 번이면, AI가 나만의 것이 됩니다" : "Build Your Own AI — Just Click, No Code"}
  </h2>

  <p class="mt-4 text-slate-400 text-lg">
    {lang === "ko"
      ? "블록을 조립하고, 결을 따라 흐르세요. 복잡한 건 우리가 덜어냈습니다."
      : "Snap the blocks together and follow the grain. We've smoothed out the complexity for you."}
  </p>
  <div class="mt-6 flex flex-col sm:flex-row justify-center gap-4">
    <Link href={lang === "ko" ? "/ko/templates" : "/templates"} style="inverted">
      {lang === "ko" ? "AI 만들기 시작하기" : "Start Building AI"}
    </Link>

    {lang === "ko" ? (
      <Link href="#" style="inverted" class="pointer-events-none opacity-50">
        철학을 글로 담았습니다. (곧 공개)
      </Link>
    ) : (
      <Link
        href="/(eng)white_paper.pdf"
        style="inverted"
        target="_blank"
        rel="noopener noreferrer"
      >
        Our Philosophy
      </Link>
    )}

    <Link href={lang === "ko" ? "/ko/about" : "/about"} style="outline">
      {lang === "ko" ? "브랜드의 결, 지금 읽기" : "Read the Brand Grain"}
    </Link>
  </div>
</section>
